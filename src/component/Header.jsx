const Header = ({title}) => {
  return (
    <header className="relative padding bg-primary">
        <div className="max-container">
            <h1 className="font-fonF text-white text-4xl font-bold h-[10vh]">
            Shop
            </h1>
        </div>
    </header>
  );
}

export default Header